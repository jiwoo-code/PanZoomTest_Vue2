{
  // Better Status Bar
  "betterStatusBar.defaultColor": "statusBar.foreground",
  "betterStatusBar.configurationFile": true,
  "betterStatusBar.loadNpmCommands": false,
  "betterStatusBar.reloadButton": "Refresh Status Bar",

  "betterStatusBar.commands": [
    {
      // 1) 브랜치 삭제 UI 호출 (VS Code 내장 명령 실행)
      "id": "delete-branch",
      "label": "🗑 Delete Branch",
      "tooltip": "Git: Delete Branch...",
      "color": "orange",
      "command": "workbench.action.git.deleteBranch",
      "useVsCodeApi": true
    },
    {
      // 2) 원격 브랜치 프룬 + 병합된 로컬 브랜치 정리 (cmd에서 실행)
      "id": "clean-branches",
      "label": "🧹 Clean Branches",
      "tooltip": "Prune remote & delete merged local branches (keeps main/master)",
      "color": "yellow",
      "command": "cmd /c \"git fetch -p && for /f %i in ('git branch --merged ^| findstr /v \\\"* main master\\\"') do git branch -d %i\"",
      "terminal": {
        "name": "branch-clean",
        "focus": false,
        "singleInstance": true,
        "clear": false
      }
    }
  ]
}