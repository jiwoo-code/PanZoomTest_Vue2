Command Runner(edonet)으로 사용

{
  "command-runner.showButton": true,
  "command-runner.commands": {
    // 1. 브랜치 삭제 (VS Code 내장 명령 호출)
    "🗑 Delete Branch": "workbench.action.git.deleteBranch",

    // 2. 병합된 브랜치 정리 + 원격 브랜치 동기화
    "🧹 Clean Git Branches": "cmd /c \"git fetch -p && for /f %i in ('git branch --merged ^| findstr /v \\\"* main master\\\"') do git branch -d %i\""
  }
}